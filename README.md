# ThoughtCanvas
English — ThoughtCanvas is a free, open-source desktop mind-mapping tool. It offers two structures — brace maps and spider-web maps — with deep customization (swappable UI skins, custom colors &amp; styles) and a local-first, open file format (.bmap, plain JSON). No forced login, no cloud lock-in — your data stays on your machine. 
